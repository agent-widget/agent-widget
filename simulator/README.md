# Simulator

This directory will hold a reproducible PC LVGL simulator. It deliberately does not contain the current partial `sim/lvgl-sim` copy or the upstream LVGL clone.

The simulator validates UI layout and interaction; performance acceptance belongs to the physical ESP32-S3.
