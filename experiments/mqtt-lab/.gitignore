# agent-widget MQTT lab — local-only state and generated artifacts.
# Reproducible configuration (broker conf, ACL, compose, sims, schemas, README)
# is committed; everything below is generated or machine-local.

# Broker runtime state (users, persisted sessions, queued messages, data)
broker/state/

# TLS certificates generated by broker/scripts/gen-certs.sh (self-signed test CA)
broker/certs/
broker/state/passwd

# Python venv created by scripts/setup.sh
.venv/

# Demo run logs
logs/
